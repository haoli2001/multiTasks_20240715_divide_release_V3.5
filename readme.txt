此版本为多通道可用版本。加入了对船体目标吃水线深度设置的支持，同时支持设置船体摇晃三个角度的设置。
匹配上位机SonarSim_UI_221107版本，上位机中已实现读txt文件接入多通道信息。

configStruct与calcResult结构体有改动。
针对上位机的启停逻辑，在进行角度循环的时候进行了一些对起始alpha角的判断，以确定是否需要从continue_alpha角度重启。

22.11.7
：更改了configStruct结构体，在计算开始前对模型各点的坐标进行了旋转变换。  在声线追踪模块中，判断各声线与面元交点坐标，水线上的直接丢弃，不参与后续计算。

24.7.15
由于0413版本是基于此版本的切分，但是结果有错误，此版本实在20221105版本上重新新建切分
viltrualface_gpu.cu：150行的报错是常见的，不影响结果，由于管线分裂等原因造成，后续有时间可以看看